nelmio_api_doc:
    documentation:
        info:
            title: Takeaway API
            description: API for Takeaway restaurants
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
